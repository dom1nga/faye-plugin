

$(function() {
  var faye = new Faye.Client("http://<%= Rails.configuration.faye_host %>:<%= Rails.configuration.faye_port %>/faye");
  faye.subscribe('/listener', function (data) {
    eval(data);
  });
});
